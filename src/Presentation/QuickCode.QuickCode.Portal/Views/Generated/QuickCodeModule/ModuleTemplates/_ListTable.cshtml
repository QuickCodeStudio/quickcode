@using QuickCode.QuickCode.Portal.Models.QuickCodeModule
@using QuickCode.QuickCode.Portal.Helpers
@inject IPortalPagePermissionManager PortalPagePermissions
@model QuickCodeModule.ModuleTemplateData
@{
    int startIndex = ((Model.CurrentPage - 1) * Model.PageSize) + 1;
    int endIndex = ((Model.CurrentPage) * Model.PageSize);
    if (endIndex > Model.NumberOfRecord) {
        endIndex = Model.NumberOfRecord;
    }

    var pagerData = new PagerData() { CurrentPage = Model.CurrentPage, NumberOfRecord = Model.NumberOfRecord, PageSize = Model.PageSize, TotalPage = Model.TotalPage, StartIndex = startIndex, EndIndex = endIndex };
    var areaName = ViewContext.RouteData.Values["Area"]?.ToString() ?? string.Empty;
    var controllerName = ViewContext.RouteData.Values["Controller"]?.ToString() ?? string.Empty;
    var actionName = ViewContext.RouteData.Values["Action"]?.ToString() ?? string.Empty;
    var ops = await PortalPagePermissions.GetPagePermission($"{areaName}{controllerName}", actionName);
}
@if (Model.List.Count == 0)
{
    <div class="alert alert-danger" role="alert">
        No Data available in QuickCodeModule.ModuleTemplate table.
    </div>
}
else
{
    <vc:pager pager-data=pagerData></vc:pager>
    <div class="table-responsive portal-table-scroll">
        <table bc-border="Bordered" bc-striped="true" bc-hover="true" class="table" bc-theme="Light">
            <thead bc-theme="Dark">
                <tr>
                    @PortalOperationButtonsHtml.Cell(ops, "Header", areaName, controllerName)
                    <th class="portal-col" data-col-type="text" data-col-key="Key">Key</th>
<th class="portal-col" data-col-type="text" data-col-key="Name">Name</th>
<th class="portal-col" data-col-type="thumb" data-col-key="TemplateImage">Template Image</th>
<th class="portal-col" data-col-type="text" data-col-key="Description">Description</th>
<th class="portal-col" data-col-type="bool" data-col-key="IsWebProject">Is Web Project</th>
<th class="portal-col" data-col-type="large" data-col-key="Dbml">Dbml</th>
<th class="portal-col" data-col-type="large" data-col-key="InitialData">Initial Data</th>
<th class="portal-col" data-col-type="int" data-col-key="Order">Order</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.List)
                {
                    <tr>
                        @PortalOperationButtonsHtml.Cell(ops, item.GetKey(), areaName, controllerName)
                        <td class="portal-col" data-col-type="text" data-col-key="Key">@item.Key</td>
<td class="portal-col" data-col-type="text" data-col-key="Name">@item.Name</td>
<td class="portal-col" data-col-type="thumb" data-col-key="TemplateImage"><a href="@(item.TemplateImage == null || item.TemplateImage.Length == 0 ? "/images/no_image.png" : item.TemplateImage)" data-toggle="lightbox" data-type="image" class="portal-list-thumb-link">
   <img class="lazyload img-fluid" src="/images/loading.svg" data-src="@(item.TemplateImage == null || item.TemplateImage.Length == 0 ? "/images/no_image.png" : item.TemplateImage)" style="height:auto;  width:auto; max-height: 110px; max-width: 110px;display: block;margin-left: auto;margin-right: auto;" bc-thumbnail="true" bc-round="true" />
</a>
</td>
<td class="portal-col" data-col-type="text" data-col-key="Description">@item.Description</td>
<td class="portal-col" data-col-type="bool" data-col-key="IsWebProject">@if (item.IsWebProject == true)
{
    <div class="ios-switch-wrapper checked">
        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_item_IsWebProject" checked disabled />
    </div>
}
else
{
    <div class="ios-switch-wrapper">
        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_item_IsWebProject" disabled />
    </div>
}
</td>
<td class="portal-col" data-col-type="large" data-col-key="Dbml">@item.Dbml</td>
<td class="portal-col" data-col-type="large" data-col-key="InitialData">@item.InitialData</td>
<td class="portal-col" data-col-type="int" data-col-key="Order">@(((object)item.Order).ToPortalGridInt())</td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
}
