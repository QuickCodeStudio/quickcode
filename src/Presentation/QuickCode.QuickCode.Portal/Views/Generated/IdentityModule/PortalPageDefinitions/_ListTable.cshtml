@using QuickCode.QuickCode.Portal.Models.IdentityModule
@using QuickCode.QuickCode.Portal.Helpers
@inject IPortalPagePermissionManager PortalPagePermissions
@model IdentityModule.PortalPageDefinitionData
@{
    int startIndex = ((Model.CurrentPage - 1) * Model.PageSize) + 1;
    int endIndex = ((Model.CurrentPage) * Model.PageSize);
    if (endIndex > Model.NumberOfRecord) {
        endIndex = Model.NumberOfRecord;
    }

    var pagerData = new PagerData() { CurrentPage = Model.CurrentPage, NumberOfRecord = Model.NumberOfRecord, PageSize = Model.PageSize, TotalPage = Model.TotalPage, StartIndex = startIndex, EndIndex = endIndex };
    var areaName = ViewContext.RouteData.Values["Area"]?.ToString() ?? string.Empty;
    var controllerName = ViewContext.RouteData.Values["Controller"]?.ToString() ?? string.Empty;
    var actionName = ViewContext.RouteData.Values["Action"]?.ToString() ?? string.Empty;
    var ops = await PortalPagePermissions.GetPagePermission($"{areaName}{controllerName}", actionName);
}
@if (Model.List.Count == 0)
{
    <div class="alert alert-danger" role="alert">
        No Data available in IdentityModule.PortalPageDefinition table.
    </div>
}
else
{
    <vc:pager pager-data=pagerData></vc:pager>
    <div class="table-responsive portal-table-scroll">
        <table bc-border="Bordered" bc-striped="true" bc-hover="true" class="table" bc-theme="Light">
            <thead bc-theme="Dark">
                <tr>
                    @PortalOperationButtonsHtml.Cell(ops, "Header", areaName, controllerName)
                    <th class="portal-col" data-col-type="text" data-col-key="Key">Key</th>
<th class="portal-col" data-col-type="fk" data-col-key="ModuleName">Module Name</th>
<th class="portal-col" data-col-type="fk" data-col-key="ModelName">Model Name</th>
<th class="portal-col" data-col-type="text" data-col-key="PageAction">Page Action</th>
<th class="portal-col" data-col-type="text" data-col-key="PagePath">Page Path</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.List)
                {
                    <tr>
                        @PortalOperationButtonsHtml.Cell(ops, item.GetKey(), areaName, controllerName)
                        <td class="portal-col" data-col-type="text" data-col-key="Key">@item.Key</td>
<td class="portal-col" data-col-type="fk" data-col-key="ModuleName">			@string.Format("{0}", Model.GetComboBoxValue("Modules", item.ModuleName))</td>
<td class="portal-col" data-col-type="fk" data-col-key="ModelName">			@string.Format("{0}", Model.GetComboBoxValue("Models", item.ModelName))</td>
<td class="portal-col" data-col-type="text" data-col-key="PageAction">@item.PageAction</td>
<td class="portal-col" data-col-type="text" data-col-key="PagePath">@item.PagePath</td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
}
