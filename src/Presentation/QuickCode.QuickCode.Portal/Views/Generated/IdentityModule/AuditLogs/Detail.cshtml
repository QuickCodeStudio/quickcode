@using QuickCode.QuickCode.Portal.Models.IdentityModule
@model IdentityModule.AuditLogData
<div class="modal-content portal-entity-modal" data-operation="Detail">
    <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Audit Log Detail Form</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
        </button>
    </div>
    <div class="modal-body">
     <div class="card" IsPageAvailable="true">
        <div class="card-body">
            <form bc-label-width-sm="4" bc-horizontal="true" id="formDetail" action="Detail" method="post" class="needs-validation portal-entity-form" bc-validation="true">
                @Html.AntiForgeryToken()
                <form-group>
	<input type="text" bc-label="Entity Name" asp-for="SelectedItem.EntityName" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Entity ID" asp-for="SelectedItem.EntityId" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Action" asp-for="SelectedItem.Action" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="User ID" asp-for="SelectedItem.UserId" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="User Name" asp-for="SelectedItem.UserName" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="User Group" asp-for="SelectedItem.UserGroup" bc-static="true" />
</form-group>

<div class="form-group row">
    <label class="col-sm-4 col-form-label" for="SelectedItem_Timestamp">Timestamp</label>
    <div class="col-sm-8">
        <input type="text" class="form-control portal-datetime-readonly" id="SelectedItem_Timestamp" readonly
            value="@(Model.SelectedItem.Timestamp is DateTime SelectedItem_Timestamp_dt && SelectedItem_Timestamp_dt.Year > 1 ? SelectedItem_Timestamp_dt.ToString("yyyy-MM-dd HH:mm:ss", System.Globalization.CultureInfo.InvariantCulture) : string.Empty)" />
    </div>
</div>

<form-group>
	<input type="text" bc-label="Duration Ms" asp-for="SelectedItem.DurationMs" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Status Code" asp-for="SelectedItem.StatusCode" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Request Module" asp-for="SelectedItem.RequestModule" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Request Path" asp-for="SelectedItem.RequestPath" bc-static="true" />
</form-group>

<div class="form-group row">
    <label class="col-md-4 control-label">Old Values</label>
    <div class="col-md-8">
        <div id="jsonEditorRO_SelectedItem_OldValues" class="jsoneditor-class" style="width: 100%; height: 300px;"></div>
        <div class="input-group">
            @Html.TextAreaFor(m => m.SelectedItem.OldValues,
            new { rows = "20", @readonly=true ,  style = "height:1px;width:1px;display:none;",
            placeholder = Html.DisplayNameFor(m => m.SelectedItem.OldValues),     
            @class = "form-control input-lg" })
        </div>
    </div>
</div>

<div class="form-group row">
    <label class="col-md-4 control-label">New Values</label>
    <div class="col-md-8">
        <div id="jsonEditorRO_SelectedItem_NewValues" class="jsoneditor-class" style="width: 100%; height: 300px;"></div>
        <div class="input-group">
            @Html.TextAreaFor(m => m.SelectedItem.NewValues,
            new { rows = "20", @readonly=true ,  style = "height:1px;width:1px;display:none;",
            placeholder = Html.DisplayNameFor(m => m.SelectedItem.NewValues),     
            @class = "form-control input-lg" })
        </div>
    </div>
</div>

<div class="form-group row">
    <label class="col-md-4 control-label">Changed Columns</label>
    <div class="col-md-8">
        <div id="jsonEditorRO_SelectedItem_ChangedColumns" class="jsoneditor-class" style="width: 100%; height: 300px;"></div>
        <div class="input-group">
            @Html.TextAreaFor(m => m.SelectedItem.ChangedColumns,
            new { rows = "20", @readonly=true ,  style = "height:1px;width:1px;display:none;",
            placeholder = Html.DisplayNameFor(m => m.SelectedItem.ChangedColumns),     
            @class = "form-control input-lg" })
        </div>
    </div>
</div>

    <div class="form-group row">
        <label class="col-sm-4 col-form-label" for="SelectedItem.IsChanged">Is Changed</label>
        <div class="col-sm-8">
            <div class="input-group">
                @if (Model.SelectedItem.IsChanged == true)
                {
                    <div class="ios-switch-wrapper checked">
                        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_SelectedItem_IsChanged" checked disabled />
                    </div>
                }
                else
                {
                    <div class="ios-switch-wrapper">
                        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_SelectedItem_IsChanged" disabled />
                    </div>
                }
            </div>
        </div>
    </div>

<div class="form-group row">
    <label class="col-md-4 control-label">Change Summary</label>
    <div class="col-md-8">
        <div id="jsonEditorRO_SelectedItem_ChangeSummary" class="jsoneditor-class" style="width: 100%; height: 300px;"></div>
        <div class="input-group">
            @Html.TextAreaFor(m => m.SelectedItem.ChangeSummary,
            new { rows = "20", @readonly=true ,  style = "height:1px;width:1px;display:none;",
            placeholder = Html.DisplayNameFor(m => m.SelectedItem.ChangeSummary),     
            @class = "form-control input-lg" })
        </div>
    </div>
</div>

<form-group>
	<input type="text" bc-label="IP Address" asp-for="SelectedItem.IpAddress" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="User Agent" asp-for="SelectedItem.UserAgent" bc-static="true" />
</form-group>

<form-group>
	<input type="text" bc-label="Correlation ID" asp-for="SelectedItem.CorrelationId" bc-static="true" />
</form-group>

    <div class="form-group row">
        <label class="col-sm-4 col-form-label" for="SelectedItem.IsSuccess">Is Success</label>
        <div class="col-sm-8">
            <div class="input-group">
                @if (Model.SelectedItem.IsSuccess == true)
                {
                    <div class="ios-switch-wrapper checked">
                        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_SelectedItem_IsSuccess" checked disabled />
                    </div>
                }
                else
                {
                    <div class="ios-switch-wrapper">
                        <input class="ios-switch" type="checkbox" onchange="checkBoxChanged(this);" id="cbox_SelectedItem_IsSuccess" disabled />
                    </div>
                }
            </div>
        </div>
    </div>

<div class="form-group row">
    <label class="col-md-4 control-label">Error Message</label>
    <div class="col-md-8">
        <div class="input-group">
            @Html.TextAreaFor(m => m.SelectedItem.ErrorMessage,
            new { rows = "20", @readonly=true , style = "height:200px;",
            placeholder = Html.DisplayNameFor(m => m.SelectedItem.ErrorMessage),     
            @class = "form-control input-lg" })
        </div>
    </div>
</div>

<form-group>
	<input type="text" bc-label="Hash" asp-for="SelectedItem.Hash" bc-static="true" />
</form-group>


                <div id="actionResultDiv">
                    <h5 id="actionResultMessage">
                        Text Result
                    </h5>
                </div>
                <div class="portal-entity-footer">
                    <div class="portal-entity-actions">
                 <button type="button" data-bs-dismiss="modal" class="btn btn-portal-secondary" bc-color="Secondary">Close</button>

                    </div>
                </div>
            </form>
        </div>
    </div>
    <div class="card" IsPageAvailable="false">
        @await Html.PartialAsync("_NotAuthorized")
    </div>
</div>
</div>
<script src="/js/bootstrap-toggle.js"></script>
<script>
        var fileData = {};
        $('.textarea-editor').summernote(
        {
            height: 200
        });
       
        window.addEventListener('load', (event) => {
            init();
        });

        $("img.lazyload").lazyload();

        function checkBoxChanged(cbox) {
            console.log('Changed ' + cbox.id);
            console.log('Changed ' + cbox.checked);
            var itemId = cbox.id.replace("cbox_", "").replace(".", "_");
            $("#" + itemId).val(cbox.checked);
        }

        $('.opButtonImageUpload').change(function (e) {
            var formData = new FormData($("#formUpdate")[0]);
            var reader = new FileReader();
            var imageId = '#' + this.id + 'Img';
            var realImageId = '' + this.id;
            imageId = imageId.replace('.', '_');
            
            reader.onload = function (event) {
              var image = new Image();
          
              image.onload=function(){
                  var imageMaxSize=1200;
                  var fileType = this.src.split(';')[0].split(':')[1];
                  
                  if(fileType.split('/').length>1 && fileType.split('/')[1] != 'gif'){
                      var canvas = document.createElement('canvas'),
                          max_size = imageMaxSize,// TODO : pull max size from a site config
                          width = image.width,
                          height = image.height;
                      if (width > height) {
                          if (width > max_size) {
                              height *= max_size / width;
                              width = max_size;
                          }
                      } else {
                          if (height > max_size) {
                              width *= max_size / height;
                              height = max_size;
                          }
                      }
                      canvas.width = width;
                      canvas.height = height;
    
                      canvas.getContext('2d').drawImage(image, 0, 0, width, height);
                      $(imageId).attr("src", canvas.toDataURL());
                      var resizedImage = createBlob(canvas.toDataURL());
                      var fileType = this.src.split(';')[0].split(':')[1];
                      var options = {
                            maxSizeMB: 1,
                            maxWidthOrHeight: imageMaxSize,
                            useWebWorker: true,
                            fileType: fileType,
                            initialQuality: 0.5
                      }
                  
                      imageCompression(resizedImage, options)
                            .then(function (compressedFile) {
                              fileData[realImageId] =  compressedFile;
                            })
                            .catch(function (error) {
                              console.log(error.message);
                            });
                    }
                  
              } 

              image.src=event.target.result;
            };

            reader.readAsDataURL(this.files[0]);
        });

        function createBlob(dataURL) {
          var BASE64_MARKER = ';base64,';
          if (dataURL.indexOf(BASE64_MARKER) == -1) {
            var parts = dataURL.split(',');
            var contentType = parts[0].split(':')[1];
            var raw = decodeURIComponent(parts[1]);
            return new Blob([raw], { type: contentType });
          }
          
          var parts = dataURL.split(BASE64_MARKER);
          var contentType = parts[0].split(':')[1];
          var raw = window.atob(parts[1]);
          var rawLength = raw.length;

          var uInt8Array = new Uint8Array(rawLength);

          for (var i = 0; i < rawLength; ++i) {
            uInt8Array[i] = raw.charCodeAt(i);
          }

          return new Blob([uInt8Array], { type: contentType });
        }

        var forms = document.getElementsByClassName('needs-validation');
        $("#formDetail").on('submit', function (e) {
            $("#actionResultDiv").hide();
            if ($("#formDetail").valid()) {
                var formData = new FormData(this);
                e.preventDefault(); // prevent the form's normal submission
                $("#actionResultDiv").show();
                $("#actionResultMessage").css('color', 'black');
                $("#actionResultMessage").text("Please wait...");
                Object.keys(fileData).forEach(function(fileKey) {
                    formData.set(fileKey, fileData[fileKey]);
                });
                
                addYamlEditorsToFormData(formData);
    
                let button = document.querySelector('button[type="submit"]');
                let formActionUrl = button.getAttribute('formaction');   
                
                $.ajax({
                    url: formActionUrl,
                    type: "POST",
                    dataType: "JSON",
                    data: formData,
                    processData: false,
                    contentType: false,
                    success: function (response, status)
                    {
                        $("#actionResultDiv").show();
                        $("#actionResultMessage").css('color', 'green');
                        $("#actionResultMessage").text('Successfully Completed');

                        var operation = ($('#formDetail').closest('.portal-entity-modal').attr('data-operation')
                            || 'Detail'
                            || '').toString();
                        var successMessage = (typeof getPortalCrudSuccessMessage === 'function')
                            ? getPortalCrudSuccessMessage(operation)
                            : 'Operation completed successfully.';

                        if (typeof finishPortalCrudSuccess === 'function') {
                            finishPortalCrudSuccess(successMessage, operation);
                        } else {
                            if (typeof showPortalToast === 'function') {
                                showPortalToast(successMessage, 'success');
                            }
                            document.location.reload(true);
                        }
                    },
                    error: function (xhr, desc, error)
                    {
                        $("#actionResultMessage").css('color', 'red');
                        var fallbackMessage = error || desc || 'Request failed';
                        if (typeof handlePortalAjaxError === 'function') {
                            handlePortalAjaxError(xhr, {
                                reload: false,
                                fallbackMessage: fallbackMessage
                            });
                            var payload = (typeof getPortalAjaxErrorPayload === 'function')
                                ? getPortalAjaxErrorPayload(xhr)
                                : (xhr.responseJSON || null);
                            var msg = (payload && payload.message) ? payload.message : fallbackMessage;
                            $("#actionResultMessage").text(msg);
                            return;
                        }
                        if (xhr.responseJSON && xhr.responseJSON.redirectUrl) {
                            window.location = xhr.responseJSON.redirectUrl;
                            return;
                        }
                        var msg = (xhr.responseJSON && xhr.responseJSON.message)
                            ? xhr.responseJSON.message
                            : fallbackMessage;
                        $("#actionResultMessage").text(msg);
                        if (typeof showPortalToast === 'function') {
                            showPortalToast(msg, 'error');
                        }
                    }
                });  
            }
        })

        var validation = Array.prototype.filter.call(forms, function (form) {
            form.addEventListener('submit', function (event) {

                if (form.checkValidity() === false) {
                    event.preventDefault();
                    event.stopPropagation();
                }
                form.classList.add('was-validated');
            }, false);
        });

        setTimeout(function () {
            var validation = Array.prototype.filter.call(forms, function (form) {
                $("#actionResultDiv").hide();
                form.classList.add('was-validated');
            });
        }, 50);
        
        initSearchableSelects(document.getElementById('itemDetailsContainer') || document);
</script>