// <auto-generated>
// QuickCode — DO NOT EDIT (quickcode-controlled scaffold)
// This file is overwritten on full template regen. Add user logic in separate .cs files.
// Where to put custom code: see AGENTS.md at repo root.
// </auto-generated>
using System;
using System.Collections.Generic;
using System.Linq;
using Microsoft.AspNetCore.Mvc.Rendering;
using QuickCode.Quickcode.Portal.Helpers;

namespace QuickCode.Quickcode.Portal.Models
{
    public class BaseComboBoxModel
    {
        public Dictionary<string, Dictionary<string, string>> ComboList { get; set; } = new();

        public string GetComboBoxValue(string comboKey, object itemId)
        {
            if (itemId == null)
                return string.Empty;

            var itemIdString = itemId.ToString() ?? string.Empty;
            if (string.IsNullOrEmpty(itemIdString) || !ComboList.TryGetValue(comboKey, out var comboItems) || comboItems == null)
                return itemIdString;

            if (comboItems.TryGetValue(itemIdString, out var value))
                return value;

            // Topic/FK lookups can differ by casing between seed rows; prefer display text when found.
            var caseInsensitiveMatch = comboItems.FirstOrDefault(i =>
                string.Equals(i.Key, itemIdString, StringComparison.OrdinalIgnoreCase));
            return caseInsensitiveMatch.Key != null ? caseInsensitiveMatch.Value : itemIdString;
        }
        
        public IEnumerable<SelectListItem> GetComboBoxList(string comboKey, object value)
        {
            var selectedValue = value.AsString();
            return ComboList[comboKey].Select(item => new SelectListItem()
                {
                    Selected = string.Equals(selectedValue, item.Key, StringComparison.OrdinalIgnoreCase),
                    Value = item.Key,
                    Text = item.Value
                }).ToList();
        }

    }
}