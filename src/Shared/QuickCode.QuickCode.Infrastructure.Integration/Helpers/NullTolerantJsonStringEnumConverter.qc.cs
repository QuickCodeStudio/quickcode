// <auto-generated>
// QuickCode — DO NOT EDIT (quickcode-controlled scaffold)
// This file is overwritten on full template regen. Add user logic in separate .cs files.
// Where to put custom code: see AGENTS.md at repo root.
// </auto-generated>
using System;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace QuickCode.QuickCode.Infrastructure.Integration.Helpers;

/// <summary>
/// JSON enum converter that tolerates null / invalid values so Portal list pages do not crash
/// when the API returns null for a nullable DB enum column.
/// Null → null for <c>TEnum?</c>; null/invalid → <c>default</c> for non-nullable <c>TEnum</c>.
/// </summary>
public sealed class NullTolerantJsonStringEnumConverter : JsonConverterFactory
{
    public override bool CanConvert(Type typeToConvert)
    {
        var underlying = Nullable.GetUnderlyingType(typeToConvert) ?? typeToConvert;
        return underlying.IsEnum;
    }

    public override JsonConverter CreateConverter(Type typeToConvert, JsonSerializerOptions options)
    {
        var underlying = Nullable.GetUnderlyingType(typeToConvert);
        if (underlying != null)
        {
            return (JsonConverter)Activator.CreateInstance(
                typeof(NullableEnumConverter<>).MakeGenericType(underlying))!;
        }

        return (JsonConverter)Activator.CreateInstance(
            typeof(NonNullableEnumConverter<>).MakeGenericType(typeToConvert))!;
    }

    private static bool TryReadEnum<TEnum>(ref Utf8JsonReader reader, out TEnum value)
        where TEnum : struct, Enum
    {
        value = default;
        switch (reader.TokenType)
        {
            case JsonTokenType.String:
            {
                var text = reader.GetString();
                return !string.IsNullOrWhiteSpace(text) && Enum.TryParse(text, ignoreCase: true, out value);
            }
            case JsonTokenType.Number:
            {
                if (reader.TryGetInt32(out var number) && Enum.IsDefined(typeof(TEnum), number))
                {
                    value = (TEnum)Enum.ToObject(typeof(TEnum), number);
                    return true;
                }

                return false;
            }
            default:
                return false;
        }
    }

    private sealed class NonNullableEnumConverter<TEnum> : JsonConverter<TEnum>
        where TEnum : struct, Enum
    {
        public override TEnum Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
        {
            if (reader.TokenType is JsonTokenType.Null)
            {
                return default;
            }

            return TryReadEnum<TEnum>(ref reader, out var value) ? value : default;
        }

        public override void Write(Utf8JsonWriter writer, TEnum value, JsonSerializerOptions options)
            => writer.WriteStringValue(value.ToString());
    }

    private sealed class NullableEnumConverter<TEnum> : JsonConverter<TEnum?>
        where TEnum : struct, Enum
    {
        public override TEnum? Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
        {
            if (reader.TokenType is JsonTokenType.Null)
            {
                return null;
            }

            return TryReadEnum<TEnum>(ref reader, out var value) ? value : null;
        }

        public override void Write(Utf8JsonWriter writer, TEnum? value, JsonSerializerOptions options)
        {
            if (value is null)
            {
                writer.WriteNullValue();
                return;
            }

            writer.WriteStringValue(value.Value.ToString());
        }
    }
}
